apiVersion: v1
kind: Service
metadata:
  name: axion-ingestion-service
spec:
  type: LoadBalancer
  selector:
    app: axion-ingestion-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
